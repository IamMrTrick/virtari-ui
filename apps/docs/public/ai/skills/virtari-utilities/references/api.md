# @virtari-packages/utilities API snapshot

Version: 5.0.0. Export entry points (exact package.json map):

```json
{
  ".": "./dist/utilities.css"
}
```

Use CSS entry points only if they appear in this map. Foundation CSS packages export their stylesheet at the package root. Primitive imports use subpaths.

## Public symbols

This package has no inventoried JavaScript barrel exports; use its package export map.

## Source type declarations



## Source files

- `packages/utilities/package.json`
- `packages/utilities/scripts/generate.mjs`
